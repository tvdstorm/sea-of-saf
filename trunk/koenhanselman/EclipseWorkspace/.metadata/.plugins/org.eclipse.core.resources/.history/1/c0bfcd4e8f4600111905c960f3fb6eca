/* This file was generated by SableCC (http://www.sablecc.org/). */

package saf.node;

import java.util.*;
import saf.analysis.*;

@SuppressWarnings("nls")
public final class ASingleProgramAction extends PProgramAction
{
    private final LinkedList<TLetter> _letter_ = new LinkedList<TLetter>();

    public ASingleProgramAction()
    {
        // Constructor
    }

    public ASingleProgramAction(
        @SuppressWarnings("hiding") List<TLetter> _letter_)
    {
        // Constructor
        setLetter(_letter_);

    }

    @Override
    public Object clone()
    {
        return new ASingleProgramAction(
            cloneList(this._letter_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseASingleProgramAction(this);
    }

    public LinkedList<TLetter> getLetter()
    {
        return this._letter_;
    }

    public void setLetter(List<TLetter> list)
    {
        this._letter_.clear();
        this._letter_.addAll(list);
        for(TLetter e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._letter_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._letter_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<TLetter> i = this._letter_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((TLetter) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
