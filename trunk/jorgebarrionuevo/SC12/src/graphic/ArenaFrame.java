package graphic;

import java.awt.Graphics;

import javax.swing.JFrame;

public class ArenaFrame extends JFrame {
	
	public void paint(Graphics g) {
        //g.drawImage(fighterPanelL.getImage(), 200, 200, rootPane);
        //g.drawImage(fighterPanelR.getImage(), 500, 200, rootPane);
    }
}
